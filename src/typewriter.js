import Typed from 'typed.js';

var options = {
    strings: [
        "Web developer",
        "Freelancer"
    ],
    typeSpeed: 40
  };
  
const text = document.querySelector('.text-typed');
text.textContent = 'Lol';